setup:
  addons:
    - plan: devmarinho1/bot_wpp
      as: whatsapp
    - plan: browserless/chrome:latest
      as: browser
  config:
    S3_BUCKET: my-example-bucket
build:
  docker:
    web: wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb && sudo dpkg -i google-chrome-stable_current_amd64.deb